import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = { // attr
}

const getters = { // arrow function
}

const actions = {
}

const mutations = { // short function
}

export default new Vuex.Store({
  state,
  getters,
  actions,
  mutations
})
